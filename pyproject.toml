[project]
name = "hyprid-proj"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mohamedrefaat92", email = "mohamedrefaat.1.1992@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "biopython>=1.88",
    "pandas>=3.0.6",
]

[project.scripts]
hyprid-proj = "hyprid_proj:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "marimo>=0.25.0",
]
